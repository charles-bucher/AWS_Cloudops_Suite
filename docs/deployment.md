# Deployment Guide — AWS_Cloudops_Suite

This document describes how to deploy the AWS CloudOps Suite — a hands‑on monitoring and security automation lab — using Terraform and Python automation scripts.

---

## 🧠 Prerequisites

Before deploying:

1. **AWS Account** with administrative IAM permissions  
2. **Terraform (v1.0+) installed**  
3. **Python (3.8+) installed**  
4. **AWS CLI installed and configured**  
5. **Git** installed and authenticated  

Configure AWS CLI:

```bash
aws configure
