# AWS_Cloudops_Suite — Setup Guide

This guide provides a **high-level overview** for setting up the AWS_Cloudops_Suite environment. It is intended for developers, reviewers, and portfolio viewers to understand deployment steps without diving deep into CLI commands.

---

## 1. Prerequisites

Before setup, ensure you have:

- **AWS Account** with administrative permissions
- **Terraform 1.0+** installed
- **Python 3.8+** installed
- **AWS CLI v2** installed and configured
- **Git** installed to clone the repository

---

## 2. Clone the Repository

1. Open your terminal or command prompt.
2. Clone the repo:

```bash
git clone https://github.com/charles-bucher/AWS_Cloudops_Suite.git
cd AWS_Cloudops_Suite
